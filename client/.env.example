# Leave empty for development (Vite proxy will handle /api -> localhost:5000)
# For production deployment, set to your deployed server URL (no trailing slash)
# Example: VITE_API_URL=https://eduquestjr-api.vercel.app
VITE_API_URL=
